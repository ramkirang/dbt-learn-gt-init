version: 2

sources:
  - name: jaffle_shop
    database: raw
    tables:
      - name: customers

      - name: orders
        columns:
          - name: status
            description: '{{ doc("order_status") }}'
      